/**
 * data access:
 * 1. dot notation
 * 2. bracket notation
 * 3. optional chaining
 * --------------------------------------
 * array operations:
 * 1. map ---> return a new array
 * 2. filter ---> condition on each element and return a new array for those which is match
 * 3. find ---> return a single or first element which is match the condition and if there is no element that match the condition then it will return undefined
 * 4. forEach ---> on each element but no return
 * 5. (optional) reduce
 * --------------------------------------
 * 
 * 1.class
 * 2.inheritance
 * 3.encapsulation
 */